# Twitch Changelog Monitor State

This orphan branch stores the state for the `monitor-twitch-changelog.yml` workflow.

- `.github/twitch-changelog-hash` — SHA-256 hash of the last-seen Twitch API changelog page
- Updated automatically by the monitor workflow
- Do not merge this branch into `main` or `test` — it is deliberately orphaned
